#include <stdio.h>
/* Writes each number from 0-4 on a new line */
int main() {
    int i; /* Initializing integer variable */
    for(i=0; 1<5, i++){ /* Before loop starts: set i to 0. Check if loop is less than 5, stop loop if condition is met. i is incrementing each iteration i++ */
        printf("%d\n", i); /* print variable on new line */
    }
}